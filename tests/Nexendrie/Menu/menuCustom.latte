  <nav id="{$menu->htmlId}">
    <h3>Menu</h3>
    <p>{foreach $menu as $item}{continueIf !$item->allowed}<a href="{plink $item->link}">{$item->text}</a>{if !$iterator->isLast()} {/if}{/foreach}</p>
  </nav>